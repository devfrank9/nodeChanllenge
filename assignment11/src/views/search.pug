extends base
include mixins/movie

block content
    form(method="GET")
        input(type="text" placeholder="input searching movie title" name="keyword")
        input(type="submit" value="Search")

    each movie in movies
        +movie(movie)
    else
        span No Movies not found